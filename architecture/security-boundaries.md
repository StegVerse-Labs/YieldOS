# Security Boundaries (v0.1)

YieldOS stores:
- metadata + references
- anonymized aggregates (optional)

YieldOS does NOT require:
- secrets
- raw sensitive facility telemetry by default

Operators may self-host deeper telemetry pipelines separately.

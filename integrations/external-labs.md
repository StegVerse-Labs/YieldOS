# External Labs (v0.1)

Store:
- report metadata (type, date, lab)
- report reference link or file pointer
- summary metrics (operator-entered)

Do not store restricted PII unless explicitly required by policy.
